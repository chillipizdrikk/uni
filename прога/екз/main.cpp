#include <iostream>
#include "arr_func.h"
using namespace std;


int main()
{



    /*cout << "Enter x: ";
    double x; cin >> x;
    cout << "Enter n (n>0): ";
    size_t n; cin >> n;

    double* myArr = createArray(x, n);
    printArray(myArr, n);

    size_t maxI = maxIndex(myArr, n);
    cout << "\nMax element. Index: " << maxI << " Value: " << myArr[maxI];
    cout << " Check: " << maxValue(myArr, n) << "\n\n";

    size_t minI;
    double minV = minValueAndIndex(myArr, n, minI);
    cout << "Min element. Index: " << minI << " Value: " << minV;
    cout << " Check: " << myArr[minI] << "\n\n";

    double totalSum = sumInRange(myArr, myArr + n);
    double sumThree = sumInRange(&myArr[0], &myArr[3]);
    cout << "Total sum: " << totalSum << "\n";
    cout << "Sum of 3:  " << sumThree << "\n\n";


    delete[] myArr;*/
    return 0;
}